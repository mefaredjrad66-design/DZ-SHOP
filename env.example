# Adresse de l'API. En local : ne mets RIEN (http://localhost:5000/api par défaut).
# En ligne (Netlify) : l'adresse de ton API Render, AVEC /api à la fin.
# REACT_APP_API_URL=https://ton-api.onrender.com/api
